# 项目介绍

## 要解决的问题

通过真实项目学习工程能力时，普通代码问答常见两个极端：

- 从入口逐行翻译，但没有形成请求链、数据流、边界和设计判断。
- 根据技术栈生成固定课程，却没有确认仓库是否真的使用、是否实现得值得模仿。

长期学习还会出现第三个问题：主线学习、临时问答、练习、Review、Debug 和功能实现分散在多个 Codex 任务中，彼此不知道对方做过什么，或者争用同一份进度文件。

`adaptive-engineering-learning` 的目标是：

> 让 Codex 先理解真实代码库，再由学习者确认路线；把不同学习活动放进独立但可共享部分状态的 workstream，使整个过程可以跨任务、跨会话恢复。

## 设计目标

1. 学习内容来自当前仓库，不来自固定课程模板。
2. Codex 可以主动分析和建议，但不能未经确认替用户决定课程。
3. 项目事实、课程路线、学习任务、Codex 窗口和掌握证据彼此分离。
4. 一个项目可以同时存在主线、问答、练习、Review、Debug 和实现窗口。
5. 每个窗口只写自己的状态，通过确定性同步共享高价值信息。
6. 学习过程不依赖聊天记忆，可以从 Markdown Dashboard/Handoff 和 JSON 状态恢复。
7. 项目中的不合理实现会被明确标记，而不是包装成最佳实践。
8. 只有学习者本人产生的解释、定位、预测、实现或诊断才可作为掌握证据。
9. 笔记只沉淀经过验证、具有长期复用价值的内容。

## 核心模型

```text
project    项目事实
plan       候选/正式课程路线
task       章节、练习、Review、Debug、问答或实现目标
workstream 一个 Codex 任务窗口的角色与续接位置
shared     多窗口之间接受的知识、问题、阻塞和证据
notes      长期复习价值的稳定知识
```

`task` 与 `workstream` 特意分开：

- 同一个章节可以同时被主线、问答和练习窗口引用。
- 问答窗口完成不代表章节完成。
- 实现窗口完成代码不代表学习者掌握。
- 一个窗口可以在完成交接后换到另一个任务，而不重置路线。

## 完整闭环

```text
只读项目发现
→ 项目学习地图
→ 候选计划（proposed）
→ 用户明确确认
→ 激活主线和章节任务
→ 按需打开侧线 workstream
→ 教学 / Q&A / 练习 / Review / Debug / 实现
→ 发布并同步高价值跨窗口信息
→ 更新学习者证据与任务状态
→ 生成 Dashboard、Handoff、Session 和稳定笔记
→ 暂停、恢复、复习或调整路线
```

## 多窗口协作

默认角色：

| Workstream | 责任 |
| --- | --- |
| `mainline` | 章节推进、主任务切换、掌握判断、计划级决策 |
| `qa-*` | 独立问答，不直接推进任务状态 |
| `exercise-*` | 练习引导和验收 |
| `review-*` | 代码审查和学习者修正 |
| `debug-*` | 证据驱动排障 |
| `pair-*` | 有边界的结对实现 |
| `impl-*` | 经明确授权的功能实现 |

每个窗口拥有独立 JSON 状态和 Markdown 交接卡。共享更新先写入 append-only inbox，再由 `sync` 在互斥锁内合并。计划变更永远排队等待确认。

## 状态与 Markdown 的分工

JSON 适合做机器真源：

- 校验字段和状态转换
- 原子更新
- 避免 Markdown 中多处重复进度
- 在多个 Codex 任务间进行窄范围写入

Markdown 适合人类恢复和复习：

- `dashboard.md`：项目总览
- `handoffs/*.md`：窗口续接卡
- `sessions/`：精简会话记录
- `notes/`：稳定知识

Dashboard 和 handoff 可以重新生成；稳定笔记保留人工内容。

## 不做什么

本 Skill 不会：

- 因为依赖清单出现某技术就自动开课。
- 为普通项目强行设计微服务、消息队列或复杂架构。
- 在没有用户确认时激活候选计划。
- 允许 Q&A 窗口直接标记章节完成。
- 自动接受侧线提出的计划变更。
- 因为 Codex 写完代码就标记学习者掌握。
- 为每个简单问题创建任务或笔记。
- 依赖不可用的嵌套 Skill 编排 API。

## 实现结构

```text
主 SKILL.md
├─ 发现与计划
├─ 教学工作流
├─ 多 Workstream 协作协议
├─ 状态模型与迁移
├─ 笔记与运维
└─ 使用说明

learning_state.py
├─ schema v2 校验
├─ 状态生命周期
├─ workstream / task 管理
├─ inbox / sync
├─ Markdown Dashboard / Handoff
├─ 原子写与互斥锁
└─ v1 可恢复迁移
```

状态脚本只使用 Python 标准库，避免给目标项目增加依赖。

## 适用场景

它适合：

- 通过 Go、Java、Python、前端、AI 工程等真实项目学习工程能力。
- 在同一项目中并行学习不同章节或实现不同功能。
- 需要把主线、问答、练习、Review 和 Debug 分开管理。
- 希望暂停后能从精确源码位置继续。
- 希望把有价值知识沉淀到 Markdown 或 Obsidian。

它不限定技术栈。是否学习某个主题由仓库真实内容、实现质量、学习目标和前置知识共同决定。

## 进一步阅读

- [README](README.md)
- [状态设计原理](docs/state-design-principles.md)
- [架构取舍](docs/adaptive-engineering-learning-design.md)
- [验证场景](docs/validation-scenarios.md)
