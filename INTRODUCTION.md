# 项目介绍

本 Skill 服务于通过真实项目学习工程能力的人。课程来自仓库中实际存在的入口、调用链、数据流、测试和实现质量；路线先提出、经学习者确认后开始。

## 两个主要空间

**主线学习**负责连贯讲解与章节进度。默认直接解释陌生概念和必要基础，不用连续反问代替讲解。练习时再给边界、验收标准和分级提示。

**独立问答**负责容纳打断主线的疑惑。代理把原问题、相关讲解、源码位置与断点一并传入新任务或已有问答任务，问题与答案单独保存，主线只接收简短结论。

Review、Debug、结对与实现仍可使用，但属于按意图选择的活动；不需要为每种活动开一个窗口。

## 为什么不会重头开始

调用 Skill 只加载规则，不代表初始化。只读 `resume` 根据已有状态返回下一步：

- 没有课程而且只是问题：直接答疑。
- 没有课程且想系统学习：分析并拟定路线。
- 有待确认路线：不擅自激活。
- 已有学习状态：恢复对应主线或问答断点。
- 旧版或异常状态：说明问题，保留数据，不重新创建。

问题包记录明确的源项目路径。新任务即使位于另一个工作树，也应读取同一源项目的学习状态，而不是在自己的工作目录初始化课程。

## 状态与阅读材料分工

| 内容 | 保存位置 |
| --- | --- |
| 路线及真实掌握证据 | plan.json、tasks/ |
| 各窗口的断点和最近讲解 | workstreams/ |
| 当时的问题语境与详细答案 | questions/ |
| 跨窗口简短结论 | inbox/、shared.json |
| 可复习知识 | 配置的 Markdown / Obsidian 目录 |
| 可读导航 | dashboard.md、handoffs/ |

只有发生实质变化才保存。结束时保留一项可执行的下一步。笔记整理不推动掌握状态，助手自己的正确回答也不是学习者证据。

## 能力边界

上下文来自当前可见对话、保存的断点/问题包，或明确来源的可访问任务。系统不会猜测“这段”指哪段，也不能自动读取未提交的侧边栏内容。

自动开任务需要宿主提供对应工具，并由用户明确提出单独开任务的意图。工具不可用时，保留上下文包并给出最短手动衔接方式。Python 测试可以证明状态和交接数据行为，不能证明所有宿主中的自然语言路由与 UI 调度均成功。

详细规则见 [Skill 入口](skill-src/adaptive-engineering-learning/SKILL.md)，开发者状态说明见 [状态设计](docs/state-design-principles.md)。
